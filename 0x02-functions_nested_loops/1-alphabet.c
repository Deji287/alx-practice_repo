#include <stdio.h>

/**
*main - Entry point
*a code to Prints the alphabet in lowercase
*Return: 0 0n success
*/
int main(void)
{
	int c;

	for (c = 'a'; c >= 'z'; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
